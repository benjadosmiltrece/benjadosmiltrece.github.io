<!DOCTYPE html>
<html id="html" lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="bootstrap/bootstrap.min.css">
    <title>The Hub</title>
    <link rel="shortcut icon" href="thumbs/icons/thehub.png" type="image/x-icon">
    <style>

        #night-mode{
            border-radius: 10px;
            margin: 5px 25px;
            scale: 200%;
            position: fixed;
            background-color: #00000015;
            width: fit-content;
            color: #797979;
        }

        #h33{
            margin-top: 25px;
        }
        h3{
            text-align: center;
            margin-bottom: 0px;
            margin-top: 0px;
            color: #bbbbbb;
        }
        h1{
            font-size: 400%;
            font-family: 'Times New Roman', Times, serif;
            text-align: center;
            margin-top: 25px;
            margin-bottom: 0px;
            color: #2c2c2c;
        }
        *{
            transition: 250ms;
        }

        html, body{
            background-color: #eee;
        }

        img{
            display: flex;
            justify-content: center;
            border-color: #2c2c2c;
            border-width: 3px;
            border-style: solid;
            width: 200px;
            filter: brightness(0.75) grayscale(0.25);
        }
        img:hover{
            scale: 110%;
            filter: brightness(1.0) grayscale(0.0);
        }
        button{
            display: flex;
            justify-content: center;
            margin-bottom: 25px;
            margin-top: 25px;
            width: 200px;
            margin-left: 7px;
            margin-right: 7px;
            border-style: none;
            background-color: #00000000;
        }
        #contenedorSalas{
            display: flex;
            justify-content: center;
        }
        
    </style>
</head>
<body>
    <button style="display: none;" onclick="toggleNight()" id="night-mode">☀</button>
<h1>The Hub</h1>
<h3>_______________________________________________</h3>
<h3>___________________________________________________________________________________________</h3>
<div id="contenedorSalas">
    <button id="c1" class="shadow-element"><a href="pepa/inicio.html"><img src="thumbs/pepaThumbnail2.png" alt="Pepa"></a></button>
    <button id="c2" class="shadow-element"><a href="minijuegos/escapa.html"><img src="thumbs/bolaThumbnail.png" alt="Bola"></a></button>
    <button id="c2" class="shadow-element"><a href="Random-Documentation/spawn.html"><img src="thumbs/randomdocu.png" alt="Random-docu"></a></button>
    <button id="c2" class="shadow-element"><a href="la_sala_del_fondo/index.html"><img src="thumbs/lasaladelfondo.png" alt="La-sala-del-fondo"></a></button>
</div>
<h3 id="h33">___________________________________________________________________________________________</h3>
<h3>_______________________________________________</h3>


<script>
let botonNoche = document.getElementById('night-mode');
var toogleF = true;

    function adjustBoxShadow() {
        const elements = document.querySelectorAll(".shadow-element");
        const centerX = (window.innerWidth / 2); // Calculate the center of the screen

        elements.forEach(element => {
            const rect = element.getBoundingClientRect();
            const distanceFromCenter = Math.abs(rect.left + rect.width / 2 - centerX);
            const xOffset = Math.max(0, distanceFromCenter / 20); - 4// Subtract 4px offset and adjust for the shadow effect

            // Calculate initial X-offset for the hovered effect
            const initialXOffset = Math.max(0, xOffset);

            // Add mouseover event listener to each element
            element.addEventListener("mouseover", function() {
                const newXOffset = Math.max(0, xOffset + (xOffset / 2)); // Add 2px to avoid flickering
                element.style.boxShadow = (rect.left < centerX ? -newXOffset : newXOffset) + "px 70px 2px #00000018";
            });

            // Add mouseout event listener to each element
            element.addEventListener("mouseout", function() {
                element.style.boxShadow = (rect.left < centerX ? -initialXOffset : initialXOffset) + "px 50px 2px #00000018";
            });

            // Set initial box shadow
            element.style.boxShadow = (rect.left < centerX ? -initialXOffset : initialXOffset) + "px 50px 2px #00000018";
        });
    }

    // Call the function once when the document is loaded
    document.addEventListener("DOMContentLoaded", adjustBoxShadow);

    // Call the function whenever the window is resized
    window.addEventListener("resize", adjustBoxShadow);

    function toggleNight(){
        if(toogleF == true){
            botonNoche.style.color = '#797979';
            botonNoche.style.backgroundColor = '#00000015';
        }
        else{
            botonNoche.style.color = '#797979';
            botonNoche.style.backgroundColor = '#00000015';
            html.style.backgroundColor = '#999999';
        }
    }
</script>


</body>
</html>